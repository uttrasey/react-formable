const markdown = require('../../samples').markdown.clear
const code = require('../../samples').code.clear

export const content = [markdown, code];
export const title = 'Clear';
export const link = 'Clear';
